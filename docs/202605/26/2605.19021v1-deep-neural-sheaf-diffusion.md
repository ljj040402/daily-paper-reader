---
title: Deep Neural Sheaf Diffusion
title_zh: 深度神经层束扩散
authors: "Remi Bourgerie, Sarunas Girdzijauskas, Viktoria Fodor"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19021v1"
tags: ["query:ai"]
score: 8.0
evidence: 深层图神经网络架构
tldr: 深度图神经网络受限于层数增加导致的表示崩溃。标准神经层扩散（NSD）虽理论阻止崩溃，但深层时拉普拉斯分歧信号消失。本文提出深度神经层扩散（DNSD），以层邻接算子替换拉普拉斯，并引入归一化、奇非线性与门控机制。在合成长程数据集上准确率提升高达30个百分点，真实基准持续领先，为图基础模型提供有效深度架构。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19021v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 642, \"height\": 691, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.19021v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1532, \"height\": 494, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.19021v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1777, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1781, \"height\": 559, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1037, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 884, \"height\": 872, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1427, \"height\": 533, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19021v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1783, \"height\": 597, \"label\": \"Table\"}]"
motivation: 现有NSD在深层时拉普拉斯分歧信号消失，无法有效利用深层聚合。
method: DNSD用层邻接算子替代拉普拉斯，配合归一化、奇非线性及门控以保持层间信息。
result: 合成长程数据集上准确率提升30个百分点，真实基准中持续超越GNN和NSD。
conclusion: 为图基础模型提供了有效的深度架构构建块。
---

## 摘要
深度图神经网络（GNN）对于捕捉图结构数据中的复杂依赖关系至关重要。然而，将GNN扩展到深层仍然具有挑战性，因为堆叠层会导致表示塌缩和由于重复聚合而导致的敏感性降低。尽管神经层束扩散（NSD）提供了防止这种塌缩的强理论保证，但这些保证在实践中并未实现：随着深度增加，层束拉普拉斯算子的不一致信号逐渐消失，限制了更深层的贡献。我们识别了阻碍NSD在深层有效性的机制，并提出了\emph{深度神经层束扩散}（DNSD），该方法用层束邻接算子替换层束拉普拉斯算子，以在各层之间保持信息丰富的信号。在此基础上，我们结合了归一化、奇次非线性和门控机制。为了对预期性能改进提供原则性解释，我们对比了层束扩散与图注意力机制，强调DNSD用矩阵值边函数替换标量注意力分数，并归一化节点表示而非注意力分数。我们通过实验证明，DNSD在图任务中有效利用了深度聚合，在合成长程数据集上比GNN和NSD基线高出多达30个百分点的准确率，并在实际基准测试中持续优于它们。这些结果将基于层束的架构定位为图基础模型的有前景的构建块，因为它支持有效的深层架构。

## Abstract
Deep Graph Neural Networks (GNNs) are essential for capturing complex dependencies in graph-structured data. However, scaling GNNs to depth remains challenging, as stacking layers leads to representation collapse and diminishing sensitivity due to repeated aggregation. While Neural Sheaf Diffusion (NSD) provides strong theoretical guarantees against such collapse, these guarantees do not translate to practice: as depth increases, the disagreement signal of the sheaf Laplacian vanishes, limiting the contribution of deeper layers. We identify mechanisms that hinder NSD effectiveness at depth and propose \emph{Deep Neural Sheaf Diffusion} (DNSD), which replaces the sheaf Laplacian with a sheaf adjacency operator to maintain informative signals across layers. This is complemented by normalization, odd nonlinearities, and gating. To provide a principled explanation of the expected performance improvement, we contrast sheaf diffusion to graph attention mechanisms, highlighting that DNSD replaces scalar attention scores with matrix-valued edge functions and normalizes node representations rather than attention scores. We demonstrate empirically that DNSD effectively utilizes deep aggregation in graph tasks, outperforming GNN and NSD baselines with up to 30pp accuracy on synthetic long-range datasets, and consistently outperforming them on real-world benchmarks. These results position sheaf-based architectures as a promising building block for graph foundation models by supporting effective deep architectures.

---

## 论文详细总结（自动生成）

# 深度神经层束扩散（DNSD）详细总结

## 1. 论文的核心问题与整体含义

- **研究动机**：深度图神经网络（GNN）堆叠层数会导致**表示塌缩**（oversmoothing）和**敏感性降低**（oversquashing），原因是标准消息传递依赖凸聚合（如加权平均）。现有**神经层束扩散（NSD）** 理论上通过层束拉普拉斯算子可避免塌缩，但实践深层时**分歧信号消失**，无法有效利用深层结构。
- **整体含义**：本文提出**深度神经层束扩散（DNSD）**，通过替换算子与稳定化机制，使层束架构能真正扩展到深层，为构建图基础模型（Graph Foundation Model）提供有效深度构建块。

## 2. 论文提出的方法论

### 核心思想
用**层邻接算子**替换层拉普拉斯算子，从“测量邻居差异”转为“聚合邻居依赖”，配合归一化、奇次非线性和门控，保持深层信号。

### 关键技术细节
1. **层邻接算子（Sheaf Adjacency Operator）**  
   - 定义：\( (A_F)_{uv} = F_{u⊴e}^\top F_{v⊴e} \)（节点对 \( (u,v) \) 边对应的线性映射乘积），聚合依赖而非差异。
   - 替代原有 NSD 中的拉普拉斯 \( \Delta_F X \)，使更新不会因分歧降低而消失。

2. **层归一化（Layer Normalization）**  
   - 对每个节点茎（stalk）的表示 \( X_v^{(l)} \in \mathbb{R}^{d \times f} \) 在特征维度 \( f \) 上独立归一化，稳定深层信号和梯度。

3. **奇次有界激活函数**  
   - 用 **tanh** 替换 ReLU：保持正负对称性，避免 ReLU 截断负值带来的表示偏移；有界性控制更新幅度。

4. **节点级逐茎门控（Per-stalk Gating）**  
   - 每个茎计算 sigmoid 门 \( g_{v,s} = \text{sigmoid}(w_g^\top [X_{v,s}; \bar{X}_{v,s}] + b_g) \in [0,1] \)，选择性调节该茎的更新幅度，抑制噪声积累。

### 更新公式（文字说明）
- 第 \( l \) 层：  
  1. 计算邻接算子输出 \( A_F^{(l)} X^{(l)} \)。  
  2. 通过 \( W_1, W_2 \) 变换后经 tanh 激活。  
  3. 乘以门控 \( G^{(l)} \)（沿特征维度广播）。  
  4. 与残差连接 \( (1+\epsilon^{(l)})X^{(l)} \) 合并。  
  5. 最后进行层归一化。

## 3. 实验设计

### 数据集与场景
- **合成长程数据集（社区检测）**：  
  - 3 个社区，各 500 节点，2D 高斯特征（标准差3，中心偏移）。  
  - KNN 图（k=8），逐步将 10% 边重连至跨社区节点，产生 11 个异质性水平 G0（同配）到 G10（完全异配）。  
- **真实世界异质性基准**（9个）：  
  - 小规模：Chameleon, Squirrel（过滤版）。  
  - 中规模：Actor, Minesweeper, Tolokers, Roman Empire, Amazon Ratings。  
  - 大规模：Questions, Penn94。

### 对比方法
- 基线：MLP、MPNN（Gilmer et al.）、GAT（Veličković et al.）、NSD（对角/全映射）。  
- DNSD 变体：组合邻接（Adj）、奇次激活（Odd）、门控（Gate），分别使用对角和全映射。

### 评估指标与深度范围
- 节点分类准确率。  
- 合成实验深度：2, 4, 8, 12, 16 层。  
- 真实实验深度：2, 4, 8 层（受计算资源限制）。

## 4. 资源与算力

- **合成实验**：标准 CPU 工作站，运行约 **两周**。  
- **真实实验**：Dardel HPC 集群，配备 **8个 AMD MI250X GPU**，总计 **70 GPU-core 小时**。  
- 论文明确说明了硬件资源，但未给出每轮训练的具体时间。

## 5. 实验数量与充分性

- **合成实验**：11 个异质性水平 × 5 种深度 × 6 个随机种子，结果统计平均值与标准差。  
- **真实实验**：9 个数据集 × 5 种深度 × 5 个随机种子；另包含 NSD 全/对角的基线对比。  
- **消融实验**：系统组合 Adj/Odd/Gate 三个标志位，共 8 种变体（对角/全映射两类）。  
- **充分性**：实验覆盖了关键超参数（深度、映射类型、组件存在性），随机种子固定且足够多，对比方法超参数保持一致。  
- **局限性**：作者指出真实基准最大深度仅为 8 层，无法充分探测深层行为；且仅评估节点分类任务，未覆盖图级任务或链接预测。

## 6. 论文的主要结论与发现

1. **邻接算子（Adjacency）是主导因素**：相比 NSD，DNSD 在中等异质性水平（G3–G9）提升 **20–30 个百分点**；无邻接算子的变体性能与 NSD 持平。  
2. **奇次激活（Odd）提供额外增益**：尤其在中等异质性（G2–G5）进一步改善。  
3. **门控（Gate）降低方差**：不影响峰值性能，但减少跨种子波动。  
4. **深度行为**：DNSD 在 12–16 层达到最佳，而 GAT 性能退化，NSD 饱和或下降；真实基准中 DNSD 在最大深度 8 层最佳。  
5. **对角映射优于全映射**：尽管参数量更少，对角映射在合成和真实基准上一致匹配或超越全映射的性能，表明高维线性映射非必要。  
6. **真实基准增益较小但仍一致**：如 Roman Empire +4.1pp，Penn94 +3.7pp（相对 NSD 对角）。

## 7. 优点

- **方法创新**：将层束扩散从“差分型”转为“依赖型”，从根本上解决深层信号消失问题。  
- **理论联系**：通过与图注意力机制的对比，解释为何 DNSD 在深层保持信息（矩阵值边函数 vs 标量注意力分数，表示级归一化 vs 分数级归一化）。  
- **实验全面**：既设计了可系统控制异质性的合成基准，又覆盖了规模、特征、异质性多样的真实基准。  
- **结果显著**：合成任务上提升幅度大，真实任务上一致领先，且具备统计可靠性（多随机种子）。  
- **可复现性**：提供了详细的架构标志（表4）和超参数（附录B）；固定种子，缓存数据集。

## 8. 不足与局限

- **任务覆盖有限**：仅评测节点分类，未涉及图分类、链接预测等，评估广度不足。  
- **真实基准深度受限**：最大仅 8 层，无法全面验证深层（≥16层）上的优势；作者呼吁构建更好的深度探测基准。  
- **计算成本**：全映射在高层数时参数较多，可能影响扩展性；但实验显示对角映射已足够，一定程度上缓解了此问题。  
- **理论分析缺失**：未提供深层信号保持性的严格理论边界（如线性扩散情况下的收敛性），主要依赖实验观测。  
- **应用场景限制**：仅讨论了同配/异配图，未涉及多模态图、超图等复杂结构。

（完）
