---
title: "ClaHF: A Human Feedback-inspired Reinforcement Learning Framework for Improving Classification Tasks"
title_zh: "ClaHF: 一种受人类反馈启发的强化学习框架用于改进分类任务"
authors: "Tianxiang Xu, Xiaoyan Zhu, Xin Lai, Jiayin Wang"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17458v1"
tags: ["query:ai"]
score: 9.0
evidence: 受人类反馈启发的强化学习框架用于文本分类
tldr: 文本分类模型依赖监督微调，但仅模仿实例标签，忽略样本间相对偏好关系，导致决策边界模糊和置信度校准不佳。受人类反馈启发，提出ClaHF框架，通过构建多候选预测与排序关系，将其转化为偏好信号并整合到奖励模型中进行策略优化。在八个分类任务上的实验表明，该方法显著提升多种语言模型的分类准确率与置信度校准，且无需额外人工标注。这一工作为利用偏好学习改进监督任务提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17458v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1593, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17458v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1611, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17458v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1656, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.17458v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 796, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1635, \"height\": 579, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1438, \"height\": 508, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1622, \"height\": 575, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1113, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1607, \"height\": 1479, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 781, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17458v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 813, \"height\": 304, \"label\": \"Table\"}]"
motivation: 监督微调仅模仿实例标签，无法捕捉样本间相对偏好，限制了决策边界优化和置信度校准。
method: ClaHF构建多候选预测及其排名关系，通过奖励模型联合建模Top-1优先与非最优排序，将标签转换为强化学习偏好信号。
result: 在八个分类任务上，ClaHF一致提升了多种语言模型的分类性能和置信度校准。
conclusion: 无需额外标注，通过偏好学习显著改进文本分类的准确率和校准能力。
---

## 摘要
文本分类模型通常通过监督微调（SFT）进行训练。然而，SFT本质上是从实例级标签进行行为克隆，因此未能充分捕捉样本之间的相对偏好关系，这限制了模型塑造决策边界和校准预测置信度的能力。在本文中，我们提出ClaHF，一种受人类反馈启发的强化学习（RL）框架，用于文本分类，它将偏好建模和RL优化整合到分类流程中，无需额外的人工标注。与仅依赖实例级监督的先前工作不同，ClaHF构建多个候选预测及其相对排序关系，并在奖励模型（RM）中联合建模Top-1偏好和非最优候选之间的排序。这种设计将传统的标签监督转化为直接适用于策略优化的偏好信号。我们在涵盖三类场景的八个分类任务上进行了系统评估。结果表明，ClaHF在多种语言模型（LM）上持续提高了分类性能和置信度校准。数据和代码可在https://anonymous.4open.science/r/ClaHF获取。

## Abstract
Text classification models are typically trained via supervised fine-tuning (SFT). However, SFT essentially performs behavior cloning from instance-wise labels and thus fails to adequately capture relative preference relations among samples, which limits the model's ability to shape decision boundaries and calibrate predictive confidence. In this paper, we propose ClaHF, a human feedback-inspired reinforcement learning (RL) framework for text classification that integrates preference modeling and RL optimization into the classification pipeline without requiring additional human annotations. Unlike prior work that relies solely on instance-wise supervision, ClaHF constructs multiple candidate predictions together with their relative ranking relations, and jointly models the Top-1 preference and the ordering among non-optimal candidates within a reward model (RM). This design converts conventional label supervision into preference signals that are directly applicable to policy optimization. We conduct systematic evaluations on eight classification tasks spanning three categories of scenarios. Results demonstrate that ClaHF consistently improves both classification performance and confidence calibration across diverse language models (LMs). The data and code are available at https://anonymous.4open.science/r/ClaHF.

---

## 论文详细总结（自动生成）

# 论文总结：ClaHF（受人类反馈启发的强化学习框架用于改进分类任务）

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：文本分类模型通常采用监督微调（SFT）训练。SFT本质上是行为克隆，即从实例级标签学习决策边界。这种做法忽略了样本之间隐含的相对偏好关系，导致模型在决策边界模糊、语义差异细微或标注噪声存在时，容易过拟合标签分布，出现决策不稳定、置信度校准差（overconfidence）等问题。
- **现有方案不足**：虽然强化学习从人类反馈（RLHF）在生成任务中取得成功，但直接迁移到分类面临两大障碍：
  - 分类输出空间是离散的，缺乏生成任务的丰富序列结构。
  - 构造偏好数据需要昂贵的额外人工标注，难以跨任务重用。
- **核心动机**：能否将RLHF的原理引入分类，在不额外标注的前提下，从现有分类数据中自动提取偏好信号，从而突破SFT的局限性？

## 2. 方法论

### 2.1 核心思想
ClaHF的关键洞察：标准分类数据中，真实标签隐含着人类决策时的偏好信号（例如SST-5中，标签“Positive (3)”意味着“3>4>2>1>0”的排序）。基于此，ClaHF无需人工标注，自动构建多候选预测及其排名关系，将其作为偏好数据，训练奖励模型，再采用PPO进行策略优化。

### 2.2 技术细节（四阶段流程）

**阶段1：监督微调（SFT）**
- 使用预训练语言模型（如BERT、RoBERTa）加分类头，以交叉熵损失训练基础分类器，获得强基线策略模型。

**阶段2：自动偏好数据构建**
- 为每个类别定义自然语言候选语义标签（如SST-5：0→“非常负面”，1→“负面”，……）。
- 对每个实例，将所有候选语义标签按与真实标签的语义距离升序排列，得到候选列表 \(C_i = \{c_{i1}, c_{i2}, \ldots, c_{iK}\}\)，其中\(c_{i1}\)总是真实类。
- 构建偏好数据集 \(D_{\text{pre}} = \{ (x_i, C_i) \}\)，编码了“Top-1优于其他”的隐含偏好。

**阶段3：奖励模型训练**
- 使用与策略模型相同结构的预训练编码器加线性打分头，输出标量奖励。
- **奖励模型损失**：复合排名损失
  - **Top-1损失**：强制真实候选得分高于其他：（公式(6)）
  - **成对损失**：强制非真实候选之间的相对排序：（公式(7)）
  - 总损失：\( L_{\text{rm}} = \alpha L_{\text{top1}} + (1-\alpha) L_{\text{pairwise}} \)，其中\(\alpha \in (0,1]\)平衡两项。二分类时简化为单对（公式(9)）。

**阶段4：强化学习优化（PPO）**
- 策略模型输出类别分布，采样动作（类标签），奖励模型为动作打分。
- 使用自适应KL惩罚防止策略偏离SFT分布：\(\hat{r}(x_i, a_i) = r(x_i, a_i) - \beta_i D_{\text{KL}}(p_\theta || p_{\text{ref}})\)，其中\(\beta\)动态调整以保持KL接近目标\(D_{\text{target}}\)。
- 优化PPO裁剪目标（公式(12)）+ 价值损失（公式(14)），总损失：\(L_{\text{rl}} = L_{\text{policy}} + \gamma L_{\text{value}}\).

### 2.3 算法流程总结
SFT初始策略 → 自动构建偏好数据 → RM训练（Top-1 + pairwise ranking） → PPO优化（带KL惩罚和价值函数）。

## 3. 实验设计

- **数据集与场景**：共8个数据集，涵盖三类场景
  - **二分类**：SST-2, MRPC, CoLA
  - **多分类**：AG News, SST-5, Emotion
  - **代码分类**：Devign, BigCloneBench
- **基线方法**：仅对比标准SFT（因为现有RLHF框架主要为生成任务设计，无直接可比的分类RLHF方案）
- **主干模型**：自然语言任务采用BERT, RoBERTa, OPT, T5, Qwen3；代码任务采用CodeBERT, CodeT5, CodeT5+, CodeGen。
- **评估指标**：
  - 准确率（Acc）、宏F1（F1）
  - 期望校准误差（ECE）——衡量置信度校准
  - CoLA使用Matthews相关系数（MCC）
- **实验设置**：所有模型训练10个epoch，8×RTX 4090，超参数在附录B详述。

## 4. 资源与算力

- 明确说明：“All experiments are conducted on 8 NVIDIA RTX 4090 GPUs.”
- 训练时长：未给出具体小时数，但提到使用AdamW优化器，每模型10个epoch。考虑到模型规模和数据集大小，算力投入合理（但未提供训练总时间）。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：8个数据集 × 多个骨干（共5~9种） = 大量组合（每个组合5次随机种子），结果以均值和标准误报告。
  - 消融实验：
    - RM训练目标消融（SFT vs ClaHF-Pair vs ClaHF-Top1 vs Full）
    - 价值损失权重γ的消融（附录C Table 7）
    - 偏好权重α与价值系数γ的联合网格搜索（Fig.3，α∈[0.3,1.0], γ∈[0,1.0]共16种组合的热力图）
  - 额外消融：ClaHF-Pair在不同γ下的表现（Table 8）
- **充分性与客观性**：
  - 覆盖了多种语言模型（从Base到Decoder-only，从纯NL到代码），结果一致提升，说明方法具有鲁棒性。
  - 每次使用5个随机种子，报告均值与标准误，统计可信。
  - 消融设计合理，逐步验证每个组件的贡献。
  - 在连续超参数空间上的分析展示了最优区域的结构化存在，并非偶然调参。
  - 不足之处：未与其他改进SFT的方法（如focal loss、label smoothing）比较，也未与P2C等现有偏好学习方法对比（但后者属于辅助损失而非完整RL周期）。基线仅SFT一个，略显单薄。

## 6. 主要结论与发现

1. **性能提升**：在所有8个数据集和所有骨干模型上，ClaHF在Acc、F1、ECE上一致优于SFT，平均提升：多分类Acc+0.25~1.54，ECE降低0.94~1.96；二分类Acc+0.68~0.92，ECE降低1.2~1.92；代码任务ECE降低1.2~3.33。
2. **校准改善显著**：ECE的下降幅度通常大于Acc提升，说明ClaHF不仅提高分类正确率，更显著改善了预测置信度的可靠性。
3. **偏好信号互补性**：Top-1约束与成对约束共同作用时效果最佳，单独使用任一种增益有限。
4. **稳定性**：在α∈[0.7,0.9]、γ∈[0.2,0.3]的范围内获得近优性能，存在平滑最优区域，表明超参数选择不敏感。

## 7. 优点

- **创新性**：首次将完整的RLHF管道（偏好构建 + 奖励模型 + PPO）系统性地引入文本分类，突破了分类任务中偏好学习依赖人工标注的瓶颈。
- **自动性**：利用标签本身的语义距离自动构造偏好数据，无需额外人工反馈，降低了应用成本。
- **通用性**：在二分类、多分类、代码分类上均有效，且跨多种预训练模型（BERT系列、T5、OPT、Qwen3、Code模型）表现一致。
- **实验严谨性**：多次重复、统计显著性、热力图分析、消融实验层次分明，结果可信。
- **开源**：提供了匿名代码和数据，便于复现和后续研究。

## 8. 不足与局限

- **数据质量依赖**：自动构造的偏好数据基于原始标签及其语义距离，如果原始标注本身存在噪声或偏差，RM的训练质量将受到影响。论文指出“integrating ClaHF with real human feedback is a promising direction”。
- **任务覆盖限制**：实验仅限定在单标签文本分类（包括代码分类），未涉及多标签分类、层次分类等更复杂判别任务。论文在Limitations中明确承认这一点。
- **对比基线少**：仅与标准SFT对比，未与focal loss、label smoothing、P2C等同类改进方法比较，难以量化该方法相对于其他SFT增强手段的额外收益。
- **计算开销**：需要训练SFT→RM→PPO三个阶段，相比单阶段SFT计算成本更高，论文未讨论资源开销对比。
- **理论分析欠缺**：虽然实证结果有力，但缺乏对偏好信号为何能改善决策边界和校准的理论解释（例如与贝叶斯最优决策的关系）。

（完）
